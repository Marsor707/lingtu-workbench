# 灵图工作台项目协作规范

## 项目范围

- 本项目是单用户、本地运行的跨平台图像工作台，当前前端原型使用 React、Vite 和 TypeScript。
- 后续桌面化方案以 Tauri + Python 本地服务为准；本地数据和生成文件不提交到仓库。
- UI 面向最终用户，避免展示 SQLite、数据库表、密钥链等后端实现细节。

## 开发约定

- 所有交流、文档和核心代码注释使用简体中文。
- 编写、修改、重构或测试代码前，必须加载并遵循 `karpathy-guidelines`。
- 涉及页面、交互、布局或视觉样式时，使用 `ui-ux-pro-max`；浏览器验证使用 `playwright-cli`。
- 遵循 KISS 和 YAGNI，保持改动范围与需求直接对应，不做无关重构。
- 核心流程、状态变化和关键设计必须保留简洁的中文注释。

## 本地 Python 环境

新终端不会自动激活项目虚拟环境。进入项目后执行：

```bash
source .venv/bin/activate
```

## 验证命令

```bash
npm run build
```

提交前至少确认构建通过，并检查 `git diff` 和 `git status`，不得提交 API Key、`.env`、本地数据库、生成图片或运行日志。

## Git 约定

- 提交信息使用中文，格式简洁并说明变更目的。
- 未经明确确认，不执行删除、批量覆盖、远程推送或其他不可逆操作。
- 文档、源码和设计资产可以提交；构建产物、缓存、运行时数据和敏感配置遵循 `.gitignore`。
