# 灵图工作台 v2.3.9 功能分析报告

分析日期：2026-08-30  
分析对象：参考软件 v2.3.9 Windows 发布包
分析方式：说明文档阅读、PE/导入表检查、PyInstaller 静态解包、Python 字节码反汇编、配置快照与源码级交叉核对。未启动 EXE，未调用任何远程 API，未使用真实账号做端到端验证。

## 1. 结论摘要

参考软件是一个 Windows 桌面批处理图像工作台，核心是 Tkinter 图形界面、OpenAI 兼容的生图/改图 HTTP 接口、可恢复任务队列，以及可选的离线/视觉模型合规初筛。当前版本在界面和代码中明确提供四种工作流：

- 正常生图：文件夹批量输入图片，按勾选模板批量生成。
- 改图模式：单张图片加修改要求，输出定向改图结果。
- 文生图：不需要输入图片，按文字、模板和独立尺寸生成。
- 一裂多：一张源图配多个提示词窗口，勾选的窗口分别生成结果。

它的生产约束比较强：默认/常用的 1K 二宫格、4K 2x2 四宫格和 4K 3x5 十五宫格都有固定尺寸、面板尺寸、间距和裁切安全区提示词。生成结果先落到运行目录，再由单独的合规阶段分类到 PASS、REVIEW、BLOCK、UNKNOWN_REVIEW、ERROR 等目录；程序本身不负责最终电商裁切。

直接在 macOS 上运行官方 EXE 的可行性很低。文件是未签名的 PE32+ Windows GUI，依赖 Windows Python 3.14/Tk/Tcl/pywin32 运行时。可行部署路径是 Windows x64 虚拟机、远程 Windows 主机或 RDP；Wine/CrossOver 只能作为试验方案，完整 GUI、并发和文件权限行为尚未验证。

最严重的安全问题是当前分析目录中的配置快照包含非空明文生图 API Key，而发布说明声称安装包不包含本地 Key。该 Key 的有效性和归属未验证，但应按已暴露凭据处理：先撤销/轮换，再从发布目录和重打包输入中移除，不能把它写入报告或提交仓库。

## 2. 证据边界与置信度

### 已由静态证据确认

- EXE 类型、Windows 依赖、PyInstaller 单文件结构、内置模块和资源。
- 文档描述的四种模式、固定版式、提示词窗口规则、首用配置流程。
- 字节码中界面页面、参数校验、队列/重试计划、输出目录、HTTP 路径和合规流程。
- 配置 JSON 的字段结构及当前快照值（敏感值已脱敏，仅用于分析，不代表线上状态）。

### 未由本次分析确认

- EXE 是否能在目标 Windows 机器成功启动、所有按钮是否可点击。
- 97 API、Sub 通道或自定义 endpoint 的实际连通性、账号权限、计费和返回格式。
- 真实图片生成质量、失败率、并发上限、服务端限流及结果持久化。
- 合规模型的实际判定准确率、OCR 识别率、缓存命中率和人工复核闭环。
- 当前配置快照中的 Key 是否有效，是否属于本项目或已被撤销。
- Wine/CrossOver、虚拟机、远程桌面下的完整运行结果。
- 最终裁切软件、上架系统以及设备/打印链路。

## 3. 安装包盘点

### 3.1 文件与指纹

- 发布说明：参考软件 v2.3.9 发布说明，日期 2026-08-14。
- 快速开始：参考软件随附快速开始文档。
- 版本变更：参考软件随附版本变更文档。
- 主程序：Windows 发布程序（文件名不在公开项目中保留）。
- EXE 大小：12,231,688 bytes。
- SHA-256：`92282cbb050db79e2753881e52b72a6384f38e2f2b7b8cb4c8251c8b6f055296`。
- MD5：`38d704d8140b881f48a21ead99411228`。
- PE：PE32+ x86-64，Windows GUI 子系统，未发现 Authenticode 签名目录。

### 3.2 内置运行时

发布包内部包含 Python 3.14 Windows 运行时、Tk/Tcl、OpenSSL、Pillow、NumPy、OpenAI 相关模块、pywin32/multiprocessing 支持文件，以及合规引擎、关键词库、提示词库和 JSON 配置。

EXE 使用 PyInstaller one-file 结构。静态解包结果位于：

本次分析生成的临时解包目录

该目录是本次取证产生的临时分析产物，不应视为官方发布内容。解包后包含 `main.pyc` 和约 2453 个 Python 模块字节码；主模块原始文件名为 `main.py`。

### 3.3 时间与版本异常

主程序和主要 Python 文件时间约为 2026-08-14，配置快照时间为 2026-08-18，版本存在混用风险。部署前必须重新确认配置是否与正式发布包属于同一批次，不能仅凭目录名认定版本一致。

## 4. 系统架构与运行边界

```text
Tkinter 桌面界面
  ├─ 工作台：四种模式、模板、尺寸、输入输出路径
  ├─ 队列/序列：批次、暂停/停止、导入任务和重试计划
  ├─ 合规页：预检/后检开关、阈值、并发和报告
  └─ 日志/画廊：任务状态、历史图片、打开输出目录
          │
          ├─ ApiClient：HTTP POST /v1/images/generations
          │             HTTP POST /v1/images/edits
          │
          ├─ 本地工作流：00_precheck → 01_raw_return → 02_prompts
          │               → 03_meta → 04_postcheck → 05_for_crop_*
          │
          └─ compliance_engine：文件名/关键词/OCR/视觉模型评分与归档
```

程序不是本地 GPU 推理程序。导入表未发现 CUDA/OpenCL 等 GPU 运行库，主要计算是图片读取、缩放、JPEG/base64 编码、文件复制和 HTTP 调用。远程 API 可用时，生成侧不要求本机显卡；但仍要求 Windows 文件系统、网络、证书和可写输出目录。

## 5. 用户界面与操作流程

### 5.1 页面结构

主窗口标题为“参考软件”，默认几何尺寸 1440x920，最小尺寸 1100x720。导航页面包括：

- `dashboard`：生产概览、最近图片、状态卡片和快捷入口。
- `generate`：工作台，切换四种模式并启动任务。
- `prompts`：内置/本地/自定义提示词库。
- `queue`：批次队列、任务序列和重试计划。
- `compliance`：预检/后检和合规参数。
- `logs`：日志与历史运行结果。

### 5.2 首次使用流程

1. 打开 `软件` 文件夹并双击 EXE。
2. 填写生图 API Key；合规 Key 可选。
3. 选择输入目录和输出目录。
4. 先用小批量或 Mock 场景验证，再扩大并发和批量。
5. 在工作台选择模式、模板、尺寸，启动任务。
6. 在队列/日志页查看成功、失败和重试计划；在画廊页查看已落盘图片。

### 5.3 状态与控制

- 黄色：运行中。
- 绿色：成功。
- 红色：失败或工作流错误。
- 支持停止当前工作流、停止剩余序列批次、刷新画廊、打开输出路径。
- “固定生产参数不在界面展示”意味着部分布局、队列和接口参数不会全部暴露在普通设置页。

## 6. 四种工作流

### 6.1 正常生图

输入为图片文件夹、勾选的模板和重复次数。程序为每个输入图与模板组合建立任务，可使用线程池/信号量并发调用生图接口。原图会经过预检（如启用），结果保存到 `01_raw_return`，提示词保存到 `02_prompts`，元数据保存到 `03_meta`。

### 6.2 改图模式

输入为单张图片和修改要求。代码使用 `/v1/images/edits`，支持多种 multipart 字段和上传编码组合，并按路由策略尝试 Codex/标准/Adobe 候选 endpoint。成功结果写入 `edit_result.png`（或编号 PNG），同时写 `edit_prompt.txt` 与 `edit_result.json`。

### 6.3 文生图

不需要输入图片。提示词来自文本框、模板或本地提示词库；尺寸独立于二宫格/四宫格布局。UI 预置 `1024x1024`、`1536x1024`、`1024x1536`、`1254x1254`，校验还接受 `宽x高` 格式，单边范围 256–4096。当前快照中的工作台 T2I 尺寸为 `1024x1024`，与全局布局 `four_up` 是两个不同字段。

### 6.4 一裂多

选择一张源图，添加多个提示词窗口；每行包含启用勾选框、名称、提示词和删除按钮。开始前至少需要两个“已勾选且非空”的窗口。每个有效窗口分别生成一组结果，适合同一素材的多方向变体。版本说明称 v2.3.9 修复了该模式预检读取错误批次目录的问题。

## 7. 生图接口与参数

### 7.1 通道

代码内置两个通道：

- 通道 `97`：`https://www.97api.com`，标签“97 API 通道”。
- 通道 `sub`：`https://sub.g-aisc.com`，标签“Sub 通道”。

默认通道为 `97`；合规内置 base URL 为 Sub。界面提示“仅显示通道名称，地址已内置”。这些是静态代码事实，未证明服务当前在线或属于官方生产环境。

### 7.2 文生图请求

`ApiClient.generate_image` 使用 Bearer Key，向 `POST {base_url}/v1/images/generations` 发送 JSON。常见字段包括：

- `model`：默认 `gpt-image-2`。
- `prompt`：最终提示词。
- `size`：如 `1024x1024`、`3840x2160`。
- `quality`：high/medium/low/auto。
- `response_format`：可选 `b64_json`。
- `n`：当前快照为 `1`。

请求超时错误信息明确提醒“服务端可能仍在处理”，生成主流程不会无限自动重试，以避免重复计费。失败任务会生成重试计划目录或 JSON 文件，供人工确认后重放。

### 7.3 改图请求与回退

`edit_image` 支持：

- Codex 路径 `/backend-api/codex/images/edits`；
- 标准路径 `/v1/images/edits`；
- 配置的 Adobe/custom endpoint。

上传模式可选原图、JPEG、PNG、WebP 或自动 JPEG。代码尝试 `image`/`image[]` 字段、是否带 `n`、是否带 `quality`、不同 JPEG 尺寸和极简参数组合。对 HTTP 400/404/413/415/422/429/500/502/503/504、连接中断和响应解析错误会按候选 profile 回退；所有 profile 失败后才抛出聚合异常。回退逻辑是客户端兼容策略，不等于服务端契约已验证。

### 7.4 当前配置快照

当前配置快照的非敏感字段显示：

- `generation_channel=97`，`generation_route=adobe_only`，`model=gpt-image-2`。
- `layout_mode=four_up`，`size=3840x2160`，`quality=high`。
- `response_format=b64_json`，发送 `n`，`n=1`。
- `codex_safe_mode=true`，`upload_image_mode=auto_jpeg`。
- 上传最大边约 1600，最大字节数约 1,800,000，JPEG quality 92。
- 请求 timeout 420 秒，`max_workers=30`，间隔 0.5 秒，按窗口并发。
- `workbench_mode=generate`，`generation_input_mode=image_batch`。

配置中存在非空 API Key，报告不展示其内容。配置还保留了原机器的输入和输出目录；迁移到其他机器前必须重新选择路径。

## 8. 版式、模板与提示词库

### 8.1 固定布局

- `two_up`：1129x1254，两个 1129x627 面板上下排列。固定安全区和裁切窗口由 `STRUCTURE_LOCK_1254_2UP` 提示词声明。
- `four_up`：3840x2160，2x2 四宫格，每格 1890x1050（9:5），外边距和沟槽 20px。
- `fifteen_up_test`：3840x3840，3x5 十五宫格，每格 1206x670（9:5），有固定边距和沟槽，仅测试用途。

布局提示词要求不拉伸、不跨面板、不添加标签或水印，并要求每个面板独立完成、可售卖。程序不会替代后续裁切软件进行推断式裁切。

### 8.2 模板数量与分类

静态常量计算出：基础模板 39 个，标准布局 2 个，实验布局 1 个，内置组合约 79 个；另有本地提示词 60 个、自定义提示词 5 个，当前配置数组总数为 144。主要分类为：

- 输入图模仿二创。
- 输入元素美术创作。
- 元素套用通用结构。
- 创作专用。

模板库还包含文字标签、图文、A/B 保守与创意、网页 2D 主视觉、元素艺术、元素结构迁移、美式文化等提示词组。提示词版本字符串含多个 2026-07 批次标识和 `base39_creation16_two_and_four_up_plus_one_15up_test_local60_custom5_v6` 后缀，进一步说明提示词库比 EXE 发布日期更复杂。

### 8.3 本地提示词

自定义库文件名为 `local_prompt_library.json`。保存对话框支持名称、分类和提示词；默认分类包含输入图二创、元素美术、结构套用、文字类、图文类、插画类和本地未分类。导入任务序列或重试计划时，程序保留当前 API Key 和 base URL，不从外部 JSON 覆盖凭据。

## 9. 队列、序列与恢复

- 当前任务可加入任务序列，支持更新、删除、移动、清空、选择和加载。
- 可导入任务序列 JSON；导入时去除 API Key/base URL，使用当前本地配置。
- 可导入失败重试计划；只合并失败任务，不覆盖当前凭据。
- 序列输出包含 `sequence_summary.csv`、`sequence_summary.txt` 和新的 `sequence_retry_plan_*.json`。
- 工作流可以停止当前任务或停止剩余批次；状态包括 running、done、stopped、error。
- 版本变更记录曾修复 worker 异常后界面卡在“生成中”的问题，但本次没有运行时压力测试，不能据此保证不会复现。

## 10. 合规筛查流水线

### 10.1 目录与阶段

一次运行通常创建：

```text
run_<timestamp>/
  00_precheck/
  01_raw_return/
  02_prompts/
  03_meta/
  04_postcheck/
  05_for_crop_PASS/
  05_for_crop_ALL_no_postcheck/
  task_log.csv
  task_log.jsonl
  workflow_summary.json
```

预检和后检均可关闭。关闭后程序会记录“本轮合规筛查完全关闭”，并将结果放到 `05_for_crop_ALL_no_postcheck`，提示人工或独立裁切软件继续处理。

### 10.2 离线规则

`compliance_engine.py` 会扫描常见图片扩展名，读取 EXIF 方向、尺寸和平均哈希；可选递归目录。离线检查包括文件名和 OCR 关键词匹配，关键词库覆盖品牌、影视/游戏角色、体育、奢侈品、名人等高风险类别，词条带 75–98 的严重度示例。

### 10.3 视觉模型规则

开启 GPT 审核时，图片会按最大边缩放、转 JPEG 并编码为 `data:image/jpeg;base64,...`，通过 OpenAI SDK 的 `responses` 或 `chat.completions` 发送。提示词明确声明这是“合规风险初筛”，不是法律意见或保证；输出 JSON 包括识别文字、视觉摘要、疑似 IP、风险旗标、面板审查、分数、等级、建议和安全改造方向。

### 10.4 分级与重试

- 0–34：LOW/PASS。
- 35–69：MEDIUM/REVIEW。
- 70 及以上：HIGH/BLOCK。
- GPT 返回无效或异常时回退为 UNKNOWN/REVIEW。
- 合规 GPT 审核最多 4 次尝试，针对 429、rate limit、5xx、超时等按 5/10/20 秒退避；这与生图主流程“不无限自动重试”是两套不同策略。

输出目录为 `PASS_LOW_RISK`、`REVIEW_MEDIUM_RISK`、`BLOCK_HIGH_RISK`、`UNKNOWN_REVIEW`、`ERROR`。程序用 `shutil.copy2` 复制文件，不是移动文件，因此会增加磁盘占用和敏感内容副本。

### 10.5 报告与缓存

会生成 `compliance_report_*.csv/.xlsx`、`reason_map_detail_*.csv`、`reason_map_summary_*.csv`、`reason_map_*.xlsx`。字段包含文件名、阶段、状态、分数、等级、建议、本地分数、证据、OCR 命中、疑似 IP、风险标签、面板审查、原图路径、是否使用 GPT、是否命中缓存和原始 JSON。

GPT 缓存默认可写入输出目录 `gpt_cache.json`，包含原始响应、文件名和时间。未发现文件权限加固、自动清理或脱敏机制；共享输出目录时应按敏感数据处理。

## 11. 数据流与外部依赖

### 本地读取/写入

- 读取输入图片、模板和本地提示词库。
- 写入 API 返回的 PNG/bin、提示词文本、元数据、日志、汇总、合规报告、缓存和分类副本。
- 使用 Windows 绝对路径、临时目录和可写文件夹；不适合直接复制快照到 macOS。

### 外部发送

- 生图/改图：将提示词、图片（改图时）和参数发送到配置 endpoint。
- 合规视觉审核：开启 GPT 时，将缩放后的图片内容和审核提示词发送到远程服务。

代码层面使用 HTTPS URL 和 Bearer Header，但本次没有验证证书校验、网关日志留存、服务商数据保留周期、账号权限、计费以及跨境合规。

## 12. 安全与运营风险

### 高优先级

1. **明文凭据暴露**：当前配置 JSON 有非空生图 API Key；发布说明却宣称安装包不包含 Key。应立即撤销/轮换，清除发布输入目录中的配置快照，并检查历史压缩包、备份和日志。
2. **未签名 EXE**：PE 的 Security Directory 为 0，用户无法通过 Authenticode 验证发布者和完整性。建议使用代码签名证书并提供 SHA-256 校验值。
3. **图片外发边界不清**：合规 GPT 会上传源图/生成图的缩放 JPEG；应在 UI 中明确告知、提供关闭默认值、确认数据区域和保留周期。

### 中优先级

4. **版本/配置不一致**：v2.3.9 EXE 旁的 `v2_6_1k2up` 配置晚于发布日期，固定配置文件名会导致隐式加载后续参数。
5. **报告和缓存含原始数据**：CSV/XLSX/JSON 可能包含原图路径、OCR、疑似 IP、原始模型响应和复制后的图片，需权限控制和生命周期清理。
6. **固定并发可能放大限流和费用**：快照 `max_workers=30`，改图还会尝试多个接口 profile；上线前应按账号配额做小批量压测，并将每次重试与实际请求计费对应起来。
7. **提示词不是法律保障**：禁止品牌、角色、名人等文本约束属于模型指令，不能替代版权审查、平台规则和人工复核。

### 低优先级/可维护性

8. 复制式归档而非移动式归档会增加磁盘空间和泄露面。
9. 当前快照保留旧 Windows 路径，迁移后容易出现空目录、权限错误或输出错位。
10. 15-up 布局标为测试用途，不应未经确认直接用于正式上架链路。

## 13. macOS 运行建议

### 直接运行

不建议也基本不可行。EXE 是 PE32+ Windows GUI，依赖 `USER32/COMCTL32/KERNEL32/ADVAPI32/GDI32`，内置 Windows Python/Tk/Tcl 和 Windows 扩展；macOS 原生加载不了这些组件。

### Wine/CrossOver

可尝试启动，但目前没有证据证明完整工作流可用。Tkinter 窗口、pywin32、multiprocessing、Windows 临时目录、文件权限和网络证书都需要单独验证。不能把“能弹出窗口”当作“能稳定批量生产”。

### 推荐路径

优先使用 Windows 10/11 x64 虚拟机或远程 Windows 主机，再通过共享目录或 RDP 取回结果。由于生成侧主要依赖远程 API，不需要给虚拟机配置 GPU；需要保证网络、证书、输出目录可写，并重新配置本机路径和凭据。

## 14. 建议的验收顺序

1. 撤销/轮换当前快照中的 Key，确认重新打包物不含明文凭据。
2. 固定一个干净 Windows x64 环境，核对 EXE SHA-256、签名和配置版本。
3. 用无敏感图片、单模板、单并发执行最小 Mock/测试通道，确认四种模式各能完成一次。
4. 逐项核对生成请求的 method/path、实际响应字段、PNG/base64 落盘和错误日志。
5. 单独验证预检、后检、缓存、报告字段和 PASS/REVIEW/BLOCK 归档；对 GPT 上传行为做抓包或服务端审计。
6. 再逐步增加并发、重复次数和任务序列，记录限流、重复计费、停止和重试计划行为。
7. 最后验证独立裁切软件、上架平台和产物交接，不把 `05_for_crop_*` 当成最终上架证明。

## 15. 关键源码/资源索引

- 主程序入口与界面：解包后的 `main.pyc`，原始文件名 `main.py`；入口约在源码行 5145，`RunnerApp` 约在 1854。
- HTTP 客户端：`main.py` 约 1474–1650（`ApiClient`、`generate_image`、`edit_image`）。
- 工作流与输出：`main.py` 约 4097、4257、4670、4838。
- 配置加载/保存：`main.py` 约 1195、1293、3422、3793。
- 合规引擎：参考软件内置合规模块。
- 合规提示词：参考软件内置提示词模块。
- 关键词库：参考软件内置关键词库。
- 当前配置快照：参考软件配置快照。
- 版式提示词：`prompt_bank_*` 模块及 `STRUCTURE_LOCK_1254_2UP`、四宫格/十五宫格布局模块。

## 16. 最终判断

从静态实现看，这是一个功能完整度较高、面向批量电商素材生产的 Windows 图像工作台，优势在于多模式生产、固定版式约束、任务可恢复和合规结果分流；它的主要边界在于远程 API 依赖、提示词合规不等于法律合规、输出中会保留大量原始数据，以及当前发布目录存在明文凭据和版本配置混用风险。

在完成凭据轮换、干净重打包、签名、Windows 环境验收、API/计费验证和数据留存策略确认之前，不应把它标记为“已通过生产验收”，也不应把 macOS 兼容性描述为已支持。
