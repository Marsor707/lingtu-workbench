# 灵图工作台本地跨平台复刻技术方案（定稿）

版本：v1.3
日期：2026-08-31
适用范围：单用户、本地运行、macOS/Windows/Linux 跨平台浏览器工作台
需求依据：原 Windows 版功能分析报告。原安装包和原文件夹仅作为需求参考，不作为新系统的运行时依赖。

## 1. 方案结论

新系统采用“浏览器 Web 界面 + Tauri 常驻托盘启动器 + Node/TypeScript 本地服务”的本地优先架构。Node 服务以独立 sidecar 随 Tauri 分发，同时托管前端静态资源，负责远程生图请求、任务编排、状态持久化和本地文件管理：

```text
Tauri 2
  + React
  + Vite
  + TypeScript
  + 原生 CSS（当前实现）
  + Node.js/TypeScript 本地 sidecar
  + SQLite
  + 本地文件系统
```

最终产物是 macOS、Windows、Linux 安装包，不依赖云端部署即可运行。生图服务仍然通过用户配置的远程 API 调用，但图片、任务、模板、日志和报告默认全部保存在本机。

## 2. 目标与非目标

### 2.1 本期目标

- 复刻四种工作流：正常生图、改图、文生图、一裂多。
- 支持本地图片批量导入、模板选择、提示词管理和固定版式。
- 支持任务排队、并发、停止、失败记录、待确认和重试计划。
- 支持画廊、历史记录、运行日志、元数据和合规报告。
- 预留合规预检/后检接口；本期只实现规则校验和人工复核所需的状态，不实现 OpenCV、OCR 或本地视觉模型。
- 支持本地 SQLite 和本地目录，不引入 OSS、Redis、PostgreSQL 或登录系统。
- 支持 macOS Apple Silicon/Intel、Windows x64、Linux x64 的构建和安装。

### 2.2 本期明确不做

- 用户注册、登录、组织、租户和权限中心。
- 云端图片存储、云端任务调度和在线协作。
- 多人同时编辑任务或模板。
- 云端数据库、Redis、Celery、Temporal、Kubernetes。
- 不依赖本地启动器和 Node 服务、仅靠浏览器直接访问本机文件系统的纯 Web/PWA 版本。
- 本地大模型推理和 GPU 加速要求。

## 3. 总体架构

```text
┌──────────────────────────────────────────────┐
│ Tauri Tray Launcher                        │
│  - 托盘菜单、浏览器启动、应用生命周期          │
│  - 启动/停止 Node sidecar                    │
└─────────────────┬────────────────────────────┘
                  │ 启动默认浏览器
┌─────────────────▼────────────────────────────┐
│ Browser · React + Vite + TypeScript          │
│  - 工作台、模板、队列、画廊、合规、日志        │
└─────────────────┬────────────────────────────┘
                  │ REST + SSE
┌─────────────────▼────────────────────────────┐
│ Node/TypeScript Local Service                │
│  node:http + SQLite + Job Orchestrator       │
│  Provider Adapter + Domain Services           │
└─────────────────┬────────────────────────────┘
                  │
┌─────────────────▼────────────────────────────┐
│ Local Workspace                              │
│  SQLite + source/generated images + reports   │
└──────────────────────────────────────────────┘
```

### 3.1 Tauri 启动器职责

Tauri 原生层保持轻量，不承载业务页面，只承担操作系统相关能力：

- 启动、监控和优雅关闭 Node sidecar。
- 创建常驻托盘菜单；“打开灵图工作台”调用系统默认浏览器，“退出”才停止 sidecar。
- 等待 `GET /health` 成功后再打开 `http://127.0.0.1:8765`，避免浏览器早于服务启动。
- 提供系统托盘和应用数据目录。
- 将 sidecar 的静态资源目录、数据库路径和固定本机端口作为启动参数注入。

不在 Rust 层实现生图业务、合规规则或任务状态机，避免业务逻辑分散到两种语言。

### 3.2 前端职责

React 前端只负责展示和交互：

- 表单校验和任务参数编辑。
- 图片缩略图、预览和拖拽排序。
- 任务列表、进度、错误和待确认状态。
- 模板、提示词和布局选择。
- 合规报告查看和人工确认。

所有关键校验必须在 Node 本地服务再次执行，不能只依赖前端校验。

### 3.3 Node 服务职责

- 对外提供本地 REST/SSE 接口。
- 维护 SQLite 数据和迁移。
- 创建和拆分批量任务。
- 通过任务编排器调度并发请求，限制并发和记录状态。
- 调用生图/改图供应商适配器。
- 执行请求参数整理、结果下载和基础文件校验；不承担 OpenCV/OCR。
- 写入本地工作区、日志、元数据和报告。

## 4. 技术栈定稿

### 4.1 前端与桌面

- 桌面壳：Tauri 2.x。
- UI：React + TypeScript。
- 构建：Vite。
- UI：当前实现为自定义 CSS；是否引入组件库需在页面复杂度达到阈值后单独评估。
- 状态管理：当前使用 React `useState`/`useMemo`；Zustand、TanStack Query 均未采用。
- 表单校验：当前为组件内校验；后续可按接口契约引入 Zod，不作为已采用依赖。
- 图标：当前使用 `lucide-react`。

### 4.2 Node/TypeScript 本地服务

- Web API：Node 原生 `node:http`，接口采用 REST + SSE。
- 数据校验：TypeScript 类型 + 明确的运行时边界校验；不把前端类型检查当作服务端校验。
- 数据库：SQLite，优先使用 Node SQLite 能力或轻量驱动，具体驱动在实现阶段锁定。
- 远程请求：Node 原生 `fetch`/`AbortController`。
- 任务执行：单进程异步任务编排器 + 有界并发，不引入 Redis/Celery 等外部队列。
- 结果处理：文件流下载、哈希/尺寸等基础校验和本地落盘；本期不引入 Pillow、OpenCV 或 Tesseract。
- 通道配置：初版仅支持一个默认通道，只配置 `base_url` 和 `api_key`；生图模型固定为 `gpt-image-2`。当前实现仅在任务进程内存中使用 API Key，避免写入 SQLite。

Node 版本锁定在项目声明的 LTS 主版本，使用 `package-lock.json` 保证本地和 CI 的依赖一致，不依赖用户全局 Node 包。

### 4.3 本地数据与任务

- 数据库：SQLite 3。
- SQLite 模式：WAL、外键约束、参数化 SQL。
- 任务执行：Node 异步 I/O + 有界并发队列；取消通过 `AbortController` 传递到远程请求和任务编排器。
- 进度推送：SSE。
- 包管理：npm，使用 `package-lock.json` 锁定依赖。

### 4.4 打包

- Node sidecar：使用 Node 24 SEA 单文件可执行方案，脚本生成后按平台 target triple 放到 `src-tauri/binaries/`。
- 桌面安装包：Tauri bundler。
- CI：GitHub Actions 按平台矩阵分别构建；不在 macOS 上假设可直接产出等价的 Windows sidecar。
- macOS：Apple Silicon、Intel、签名、公证。
- Windows：x64、代码签名、安装程序升级测试。
- Linux：x64，明确 WebKitGTK 依赖和发行版支持范围。

当前 Tauri 已使用 `bundle.externalBin` 声明 `binaries/lingtu-server`，将 Vite `dist` 作为 `resources/dist` 打入安装包，并通过 Shell 插件启动和在退出事件中回收 sidecar。启动器常驻托盘，健康检查成功后打开系统默认浏览器。实际文件名按平台追加 Rust target triple：`lingtu-server-aarch64-apple-darwin`、`lingtu-server-x86_64-apple-darwin`、`lingtu-server-x86_64-pc-windows-msvc.exe`。动态端口、会话 token 和 shutdown 协议仍按后续迭代实现。

GitHub Actions 使用 macOS 和 Windows runner 矩阵：macOS runner 构建 Apple Silicon/Intel 安装包，Windows runner 构建 x64 NSIS/MSI 安装包。每个 runner 先构建对应平台的 Node sidecar，再执行 `tauri build`；Windows 安装包的生成不依赖本机 Windows 开发环境。

## 5. Sidecar 启动协议

1. Tauri 启动 Node sidecar，当前使用固定本机端口 `8765`。
2. Node 仅监听 `127.0.0.1`，启动完成后向 stdout 输出一行 JSON：`{"ready":true,"port":8765}`。
3. Tauri 轮询 `GET /health`，成功后调用系统默认浏览器打开 `http://127.0.0.1:8765`。
4. 启动器保持托盘常驻；只有用户点击托盘“退出”时才回收 sidecar child 并退出。

Node 服务不得监听 `0.0.0.0`，也不得把本地接口暴露给局域网。

## 6. 领域模块划分

```text
server/
  src/
    api/
      assets.ts
      prompts.ts
      jobs.ts
      sequences.ts
      compliance.ts
      settings.ts
      events.ts
    domain/
      job-state.ts
      generation.ts
      workflow.ts
      compliance.ts
      layout.ts
    providers/
      base.ts
      openai-compatible.ts
      provider-97.ts
      provider-sub.ts
    jobs/
      orchestrator.ts
      concurrency.ts
    image/
      download.ts
      encode.ts
      layout.ts
    repositories/
    models/
    storage/
    migrations/
```

前端目录建议：

```text
frontend/
  src/
    pages/
    features/workbench/
    features/jobs/
    features/prompts/
    features/gallery/
    features/compliance/
    components/
    api/
    stores/
    schemas/
```

## 7. 四种工作流契约

### 7.1 正常生图

输入：图片集合、模板集合、布局、尺寸、质量、重复次数。  
处理：后端按“输入图片 × 模板 × 重复次数”拆分 `job_items`。  
输出：每个子任务独立记录原图、提示词、接口参数、生成结果和错误。

### 7.2 改图

输入：单张源图、修改要求、尺寸、质量和供应商路由。  
处理：Node Provider Adapter 发送 multipart 请求，按配置的接口 profile 执行。
输出：改图结果、请求元数据、耗时、响应解析结果。

### 7.3 文生图

输入：文字提示词、模板、独立尺寸和质量。  
处理：不要求 source asset；尺寸独立于二宫格/四宫格布局。  
输出：生成图片和完整提示词快照。

### 7.4 一裂多

输入：一张源图、多个提示词窗口。  
校验：至少两个启用且非空的窗口。  
处理：每个有效窗口创建独立子任务，结果按窗口名称归档。

### 7.5 生图 UX 约束

- 提交后立即进入队列，显示总进度和每个任务项的独立状态；单项失败不能把其他成功结果隐藏或回滚。
- 结果下载并完成本地落盘后才显示“成功”，预览、打开目录和元数据入口应可直接使用。
- 用户可以停止当前任务或后续队列；取消只影响未完成请求，已落盘结果保留。
- 远程请求超时或连接中断进入 `NEEDS_CONFIRMATION`，界面提供查询、确认重试和放弃，不自动重复可能计费的生图请求。
- 禁止无限自动重试；重试必须由用户明确触发并沿用原任务上下文，避免重复创建任务。
- 四种工作流共享同一队列、进度、失败、待确认和画廊体验，差异只体现在输入参数和 Provider 请求形态。

## 8. 任务状态机

```text
CREATED → QUEUED → RUNNING → SUCCEEDED
                         ├── FAILED
                         ├── CANCEL_REQUESTED → CANCELLED
                         └── NEEDS_CONFIRMATION
```

### 状态规则

- `CREATED`：参数已保存，尚未进入执行队列。
- `QUEUED`：等待本地任务编排器。
- `RUNNING`：已发起处理或远程请求。
- `SUCCEEDED`：结果已落盘并写入数据库。
- `FAILED`：明确失败，包含可重试原因和错误摘要。
- `CANCEL_REQUESTED`：用户请求停止，编排器通过 `AbortController` 取消未完成请求，并在安全点结束已落盘项。
- `CANCELLED`：未继续执行的任务。
- `NEEDS_CONFIRMATION`：请求超时/连接中断，无法判断服务端是否已生成。

生图请求超时不得自动重试。用户必须显式选择查询、确认重试或放弃，避免重复计费。合规审核可以使用有限次数退避重试。

## 9. SQLite 数据模型

### assets

- `id`
- `relative_path`
- `kind`：source/generated/report
- `sha256`
- `file_size`
- `width`
- `height`
- `mime_type`
- `created_at`

### provider_channels

默认通道配置表，初版只支持单用户本地使用。当前实现不把 `api_key` 写入 SQLite，应用层不得将其回传给前端、写入日志或导出文件；模型固定为 `gpt-image-2`，无需单独存储可编辑模型字段。

- `id`
- `base_url`
- `api_key`
- `created_at`
- `updated_at`

### prompt_templates

- `id`
- `name`
- `category`
- `prompt`
- `layout_mode`
- `is_builtin`
- `version`
- `created_at`
- `updated_at`

### jobs

- `id`
- `mode`：generate/edit/text_to_image/one_to_many
- `channel_id`：创建任务时使用的通道快照引用
- `status`
- `layout_mode`
- `size`
- `quality`
- `repeat_count`
- `total_items`
- `completed_items`
- `failed_items`
- `retry_plan_id`
- `created_at`
- `started_at`
- `finished_at`

### job_items

- `id`
- `job_id`
- `source_asset_id`
- `template_id`
- `prompt_snapshot`
- `one_to_many_name`
- `status`
- `attempt_count`
- `provider_request_id`
- `error_code`
- `error_message`
- `started_at`
- `finished_at`

### job_outputs

- `id`
- `job_item_id`
- `asset_id`
- `output_type`
- `width`
- `height`
- `raw_response_path`
- `metadata_path`
- `created_at`

### compliance_reviews（本期预留）

- `id`
- `asset_id`
- `stage`
- `risk_score`
- `risk_level`
- `recommendation`
- `ocr_text`：后置 OCR 插件字段，本期保持为空
- `keyword_hits_json`
- `suspected_ip_json`
- `model_raw_json_path`
- `cache_hit`
- `reviewed_at`

## 10. 本地工作区

```text
灵图工作台工作区/
  workspace.db
  assets/
    source/
    generated/
  runs/
    run_YYYYMMDD_HHMMSS_xxx/
      00_precheck/
      01_raw_return/
      02_prompts/
      03_meta/
      04_postcheck/
      05_for_crop_PASS/
      05_for_crop_REVIEW/
      05_for_crop_BLOCK/
  reports/
  logs/
  backups/
```

数据库只保存工作区相对路径，不保存平台相关的绝对路径。导入任务序列时，如果路径不存在，前端显示待重新绑定，不直接执行。

图片不存储为 SQLite BLOB。应用退出或任务完成后，不自动删除原图和结果，删除功能必须由用户主动执行，并在日志中记录。

## 11. 接口设计

### 11.1 任务

```text
POST /api/jobs
GET  /api/jobs
GET  /api/jobs/{job_id}
POST /api/jobs/{job_id}/cancel
POST /api/jobs/{job_id}/confirm-retry
GET  /api/jobs/{job_id}/events
```

### 11.2 资产

```text
POST /api/assets/import
GET  /api/assets
GET  /api/assets/{asset_id}
GET  /api/assets/{asset_id}/open
DELETE /api/assets/{asset_id}
```

### 11.3 模板和序列

```text
GET  /api/prompt-templates
POST /api/prompt-templates
PUT  /api/prompt-templates/{id}
DELETE /api/prompt-templates/{id}

GET  /api/sequences
POST /api/sequences
PUT  /api/sequences/{id}
POST /api/sequences/{id}/run
POST /api/sequences/{id}/stop
```

### 11.4 合规和报告

```text
POST /api/compliance/reviews
GET  /api/compliance/reviews/{id}
GET  /api/reports/{id}
```

### 11.5 默认通道配置

```text
GET  /api/settings/channel
PUT  /api/settings/channel
POST /api/settings/channel/test
```

请求体仅包含 `base_url` 和 `api_key`。读取接口只返回脱敏后的密钥或 `api_key_configured` 状态；完整密钥只在本地服务进程内用于请求注入。模型固定为 `gpt-image-2`，不由前端传入。

所有创建任务接口应支持客户端生成的 `idempotency_key`，避免用户重复点击导致任务重复创建。

Node 原生 `node:http` 只负责协议和连接生命周期，业务路由保持按模块拆分。`GET /api/jobs/{job_id}/events` 使用 `text/event-stream` 推送 `job.created`、`job.progress`、`job.item.updated`、`job.completed` 和 `job.error`；客户端断线后先用详情接口回读，再从最新事件继续订阅，不能仅依赖事件流恢复状态。相同用户会话下重复提交同一 `idempotency_key` 必须返回第一次创建的任务，不得再次调用 Provider。

## 12. 生图供应商适配

供应商接口必须封装在 `providers/`，业务层不能直接拼接 URL。

统一适配器接口（TypeScript）：

```ts
generate(request: GenerationRequest, signal?: AbortSignal): Promise<GenerationResult>
edit(request: EditRequest, signal?: AbortSignal): Promise<GenerationResult>
healthCheck(signal?: AbortSignal): Promise<ProviderHealth>
```

适配器负责：

- endpoint 和路径。
- Bearer Key 注入。
- JSON/multipart 参数。
- 图片编码和尺寸限制。
- 响应字段解析。
- 超时、限流和错误分类。
- 请求 ID 和原始响应摘要。

每次执行由 Node 本地服务接收当前通道配置，使用 `base_url` 拼接接口地址，并仅在 Provider Adapter 内部注入 `api_key`。API Key 允许在设置表单中短暂编辑，但不进入任务业务状态、SQLite 请求快照、日志和错误信息；未知超时统一转为 `NEEDS_CONFIRMATION`，不自动切换其他供应商，也不自动重复扣费请求。

## 13. 合规流水线（本期范围）

```text
输入图片
  ↓
文件名/关键词规则（本期）
  ↓
人工复核状态（本期）
  ↓
后置可插拔检测器（未来：OCR/视觉模型/OpenCV）
  ↓
风险评分
  ├─ PASS
  ├─ REVIEW
  ├─ BLOCK
  ├─ UNKNOWN_REVIEW
  └─ ERROR
```

本期只复刻状态和报告契约，不承诺 OCR 或视觉判定能力。后置插件接入后可复用以下阈值：

- 0–34：LOW/PASS。
- 35–69：MEDIUM/REVIEW。
- 70 及以上：HIGH/BLOCK。

合规结果必须标记“风险初筛”，不能表述为法律结论。未来开启视觉模型或第三方 OCR 前，界面必须显示图片将被发送至第三方服务，并允许用户关闭该阶段。OpenCV/OCR 作为独立检测器接入，不得阻塞生图主流程。

## 14. 前端页面定稿

- 工作台：四种模式、源图、模板、尺寸、质量、布局和启动按钮。
- 提示词库：内置、用户自定义、分类、搜索和收藏。
- 任务队列：当前任务、任务明细、进度、失败、停止和待确认。
- 任务序列：批次排序、编辑、删除、运行和停止。
- 画廊：缩略图、筛选、预览、打开所在目录。
- 合规：预检/后检开关、阈值、报告和人工复核。
- 日志：运行日志、错误摘要、原始请求信息脱敏展示。
- 设置：单个默认通道的 `baseUrl`、`apiKey`、默认路径和并发上限；模型固定为 `gpt-image-2`。当前仅持久化地址，API Key 在会话内存中保留。

## 15. 安全要求

- 默认通道地址可保存在前端本地存储，API Key 不落盘，避免在未接入系统钥匙串前扩大暴露面。
- SQLite 文件仅供当前本地用户访问；API Key 仅在设置表单编辑期间短暂存在，不进入任务业务状态、任务导出、日志和错误信息。后续如有更高安全要求，再评估 SQLite 加密，不改变通道配置接口。
- 本地服务仅监听 `127.0.0.1`。
- 每次启动生成新的本地会话 token。
- 日志禁止输出完整 URL 查询参数、Authorization Header 和原始 Key。
- 导入任务序列和重试计划时剥离凭据。
- 文件路径统一使用工作区相对路径。
- 图片和报告删除前需要用户确认。
- 合规原始 JSON、后置插件产生的 OCR 文本和复制图片属于敏感本地数据。
- 发布包必须签名并提供 SHA-256。
- macOS 发布包需要签名/公证，Windows 安装包需要代码签名。

## 16. 测试方案

### 单元测试

- 四种工作流参数校验。
- 布局尺寸和裁切窗口计算。
- 任务状态转换。
- 重试计划生成和导入。
- provider 响应解析。
- 合规评分和阈值分类。
- SQLite 迁移。

### 集成测试

- Mock 生图服务，不访问真实供应商。
- 真实图片从导入到结果落盘的完整流程。
- 应用重启后的任务恢复。
- 超时进入 `NEEDS_CONFIRMATION`。
- 停止任务和序列剩余批次。
- 后置合规插件缺失时，主流程仍可生成并将检测状态标记为未执行。

### E2E 测试

- Playwright 验证四种模式切换、创建任务、查看进度和画廊。
- macOS、Windows、Linux 各执行最小批量手工验收。
- 每个平台验证文件选择、路径编码、安装升级和卸载残留。

CI 不使用真实 API Key，不上传真实用户图片，不执行真实计费请求。

## 17. 实施阶段

### 阶段一：基础壳和数据层

- 建立 Tauri + React + Vite 项目。
- 建立 Node/TypeScript sidecar 启动协议。
- 完成 SQLite schema 和迁移脚本。
- 完成工作区创建、打开和备份。
- 完成默认通道配置表、SQLite 迁移和通道配置读写。
- 完成 `baseUrl + apiKey` 格式校验和通道连通性测试。

### 阶段二：生成 MVP

- 文生图。
- 改图。
- 单张和多张导入。
- 模板管理。
- 本地画廊。
- 基础任务队列和进度。

### 阶段三：批量生产能力

- 正常生图。
- 一裂多。
- 二宫格、四宫格和十五宫格测试布局。
- 任务序列。
- 停止、恢复和重试计划。

### 阶段四：合规能力（后置插件）

- 关键词初筛。
- 预留 OCR/视觉模型检测器接口，不纳入本期主链路。
- PASS/REVIEW/BLOCK 分类。
- CSV/XLSX/JSON 报告。

### 阶段五：跨平台发布

- 通过 GitHub Actions 在 macOS/Windows/Linux runner 分别构建 sidecar。
- 安装包签名、公证和升级。
- 多架构图片处理依赖验证。
- 最小批量、异常退出和路径兼容验收。

## 18. 架构决策记录

### ADR-001：采用 Tauri 而不是 Electron

决定：采用 Tauri 作为桌面壳。  
原因：本项目是单机工具，核心耗时来自远程 API；Tauri 的低基础内存占用和较小原生壳更符合本地工具定位。
代价：需要维护少量 Rust 生命周期代码，并测试不同系统 WebView 的差异。  
例外：如果后续强制要求业务逻辑全部使用 Node.js，或团队无法维护 Rust，再评估 Electron。

### ADR-002：采用 Node/TypeScript 本地服务

决定：远程生图请求、任务编排、SSE、SQLite 和本地文件管理使用 Node/TypeScript sidecar。
原因：前后端统一类型和工具链，降低本地开发与跨平台 CI 的维护成本；本期没有 OpenCV/OCR 等必须依赖 Python 的能力。
代价：需要为各平台准备 Node sidecar 可执行文件，并自行约束异步任务、取消和并发语义。OpenCV/OCR 未来以独立可插拔检测器评估，不改变生图主链路。

### ADR-003：采用 SQLite 而不是服务端数据库

决定：任务元数据使用 SQLite；当前版本不将 API Key 写入配置表，模型固定为 `gpt-image-2`。
原因：单用户、本地运行、无云存储，不需要数据库服务进程；配置和任务数据可以在同一工作区备份。  
限制：不支持多人并发写入，不适合直接升级为在线多租户数据库；未来迁移时通过 Repository 层隔离数据库实现。

### ADR-004：通道凭据保存于 SQLite

决定：初版不接入系统钥匙串，只支持一个默认通道；`baseUrl` 可本地保存，API Key 仅会话内使用；模型固定为 `gpt-image-2`。
原因：保持单机部署简单，配置随工作区迁移和备份；前端只负责编辑，不直接发起供应商请求。  
边界：SQLite 文件属于本地敏感数据，需限制文件权限；API Key 仅在设置表单编辑期间短暂存在，禁止进入任务业务状态、日志、任务导出和错误信息。未来如需加密，可在存储层替换实现。

### ADR-005：采用本地文件系统而不是 OSS

决定：图片和报告保存在用户工作区。  
原因：当前不做云端存储，减少隐私和部署复杂度。  
限制：用户自行负责备份；跨设备访问和远程协作不在本期范围。

## 19. 验收标准

- 在 macOS、Windows、Linux 上可以安装、启动和退出。
- Node sidecar 能被 Tauri 正常启动、监控和关闭（macOS 已验证，其他平台需 CI/人工验收）。
- 不配置真实 API Key 时，应用仍可启动并能运行 Mock 流程。
- 四种模式都能创建任务并在 SQLite 中持久化状态。
- 生图请求超时不会自动重复调用，而是进入待确认状态。
- 应用异常重启后，未完成任务不会被静默标记为成功。
- 图片、日志、提示词、元数据和报告均能写入本地工作区。
- 默认通道的 `baseUrl` 可本地恢复，API Key 需在当前会话重新输入；生图请求固定使用 `gpt-image-2`。
- API Key 不出现在导出 JSON、日志、错误信息和任务业务状态中；设置读取接口只返回脱敏值或已配置状态。
- 不同平台路径、中文文件名和高分辨率图片均能通过最小批量测试。

## 20. 最终技术选型

```text
桌面壳：Tauri 2
前端：React + Vite + TypeScript
UI：自定义 CSS（当前实现）
状态：React `useState`/`useMemo`（当前实现）
本地服务：Node.js + TypeScript + 原生 `node:http`
数据校验：TypeScript + 服务端运行时边界校验
数据库：SQLite
数据库访问：轻量 SQLite 驱动（实现阶段锁定）
任务执行：Node 异步 I/O + 有界并发 + AbortController
图像：基础文件流/哈希/尺寸校验（本期）
OCR/OpenCV：不在本期，作为后置可插拔能力
远程请求：Node 原生 `fetch`
实时进度：SSE
存储：本地工作区
打包：按平台 Node sidecar + Tauri bundler
测试：Node `node:test` + Playwright
```

本方案以“本地单用户、跨平台、可恢复批处理、优先保障生图体验”为约束定稿，产品名称为“灵图工作台”。当前仓库目标实现和新增业务代码统一使用 Node/TypeScript；Python 健康检查文件仅作迁移期过渡保留。SQLite 任务元数据、Node SEA sidecar、异步任务编排、OpenAI 兼容 Provider 适配器、SSE 状态流和 79 条内置生成提示词已落地；真实 Provider、跨平台安装包和正式签名仍需在对应环境分别验证。内置提示词存放在 `src/data/builtin-prompts.ts`，不依赖参考软件目录，也不携带参考配置中的 API Key、地址或本地路径。未来如果需要多人协作，只需要替换存储、任务队列和认证层，不应改变四种工作流、任务状态机和 Provider Adapter 接口。
