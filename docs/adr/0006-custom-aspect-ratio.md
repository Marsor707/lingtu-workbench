# 自定义长宽比例使用统一像素尺寸提交

状态：已采用。工作台允许用户输入 1–100 的正整数宽高作为自定义比例；提交给图像供应商时，以最长边 1800 像素换算为整数的 `宽 × 高` 尺寸。预览展示用户输入的约分比例，任务请求和任务快照保存换算后的像素尺寸。

选择该方案是为了兼容现有 Provider 的 `size` 字段契约，同时让任意比例都能在本地预览和后端比例提示中使用。换算存在四舍五入误差，界面明确提示 Provider 可能返回最接近尺寸；自定义模式和宽高保存在浏览器本地，刷新后继续使用。
