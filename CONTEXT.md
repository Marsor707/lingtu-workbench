# 灵图工作台领域词汇

## 模型供应商

用户可配置的 Provider 连接档案，由名称、接口地址和密钥组成。供应商是任务提交时选择的执行归属。

## 启用供应商

当前用于新任务的唯一供应商。系统任意时刻最多一个启用供应商；已提交任务不因后续切换而改变归属。

## 供应商切换

将另一家供应商设为启用供应商的操作。只允许在没有执行中任务时发生，切换失败不改变原启用状态。

## 供应商统计

按任务最终状态累计的成功次数和失败次数。重复生成属于同一任务；取消任务不计入任一计数。

## 4K 像素放大

对已生成图片做本地、非模型的 Lanczos3 像素重采样。保持原图比例，将最长边提升到 3840 像素；已达到或超过该尺寸的图片不缩小。

## 放大设置快照

任务创建时记录当时是否启用 4K 像素放大，后续修改设置不影响已经排队的任务。

## 任务重试

对失败或已取消任务沿用原任务 ID，保留原提示词、源图和高级参数，改用当前最新的模型供应商与工作区设置重新执行；重试时间会更新任务创建时间，重试前清除上一轮结果引用和错误信息，但不删除磁盘上的旧文件。
_Avoid_: 新建任务、部分任务项重试、自动重试
