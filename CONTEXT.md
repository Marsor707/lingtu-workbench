# 灵图工作台领域词汇

## 模型供应商

用户可配置的 Provider 连接档案，由名称、接口地址、密钥和生图模型组成。供应商是任务提交时选择的执行归属。

## 生图模型

模型供应商执行图像生成时使用的模型标识。系统只支持内置的一组固定生图模型；新增供应商时使用默认生图模型，修改供应商的生图模型只影响之后创建的任务。生图模型是模型供应商的组成部分，切换启用供应商即切换生图模型。

_Avoid_: 当前模型（指代启用供应商时）

## 生图模型快照

任务创建时记录的生图模型。后续修改供应商的生图模型不改变已经提交的任务。

## 提示词资产

**提示词**：用于描述一次图像生成方向的可复用文本资产，包含标题、分类、正文和可选布局提示。

**内置提示词**：随应用初始化提供的系统提示词。可以编辑，但不能从提示词库删除；升级迁移不得覆盖用户已经编辑过的内容。

**自定义提示词**：用户在提示词库中新增并持久化保存的提示词。可以编辑和删除。

**提示词编辑**：对提示词标题、分类、正文或可选布局进行修改并保存到本地数据库的操作；取消不会改变已保存内容。

**提示词任务快照**：任务提交时写入任务请求的提示词正文副本。后续编辑或删除提示词不影响已提交任务。

**提示词初始化/升级迁移**：应用启动时补齐缺失的内置提示词，并仅迁移未被用户编辑过的内置记录。

## 启用供应商

当前用于新任务的唯一供应商。系统任意时刻最多一个启用供应商；已提交任务不因后续切换而改变归属。

## 供应商切换

将另一家供应商设为启用供应商的操作。只允许在没有执行中任务时发生，切换失败不改变原启用状态。

## 供应商统计

按任务最终状态累计的成功次数和失败次数。重复生成属于同一任务；取消任务不计入任一计数。

## 像素放大

对已生成图片做本地、非模型的 Lanczos3 像素重采样。三档可选：关闭、2K、4K；保持原图比例，得最长边提升到 2560（2K）或 3840（4K）像素；已达到或超过目标尺寸的图片不缩小。

## 放大设置快照

任务创建时记录当时的像素放大档位（关闭/2K/4K），后续修改设置不影响已经排队的任务。

## 任务重试

对失败或已取消任务沿用原任务 ID，保留原提示词、源图和高级参数，改用当前最新的模型供应商（含生图模型）与工作区设置重新执行；重试时间会更新任务创建时间，重试前清除上一轮结果引用和错误信息，但不删除磁盘上的旧文件。重试沿用原提交批次，结果仍写回同一个任务文件夹。
_Avoid_: 新建任务、部分任务项重试、自动重试

## 提交批次

一次「开始任务」操作对应的任务集合。一次提交可能只包含一个任务，也可能包含多个任务（例如改图一次选择多张源图）；同一提交批次的结果归入同一个任务文件夹。历史任务没有批次归属，视为各自独立成批。

## 任务文件夹

按提交批次归集生图结果的本地文件夹。一个提交批次对应一个任务文件夹，批次内所有任务的产出都写入其中；批次文件夹缺失时会在下次写入前重新创建。缺少批次归属的历史任务使用任务自身作为文件夹。

## 输出比例

**预置比例**：工作台提供的固定画布比例选项，包含 16:9、9:10、1:1、3:2 和 9:5；每个选项同时对应一个用于任务提交的像素尺寸。

**自定义比例**：用户输入宽、高两个正整数得到的画布比例。输入允许横向或纵向，选择状态和最近一次宽高保存在浏览器本地；不完整或非法时不能提交该自定义值。

**比例像素尺寸**：提交给图像供应商的 `宽 × 高` 画布尺寸。预置比例直接提供像素尺寸；自定义比例会换算为可提交的整数尺寸，任务快照和供应商请求使用该尺寸表达。

## LLM 图片命名
生图成功后，可由用户启用的视觉 LLM 根据图片内容生成文件名。命名是生图结果的旁路增强，失败时回退原有命名，不改变生图任务结果。

## LLM 供应商
用于图片命名的独立连接档案，由名称、服务地址、模型名称和密钥组成。LLM 供应商与生图供应商分别维护启用状态和成功/失败统计。

## 图片命名开关快照
任务创建时记录是否启用 LLM 图片命名及当时的 LLM 供应商，后续修改工作区设置或切换供应商不影响已提交任务。
